import 'package:flutter/material.dart';

class Blackwells extends StatelessWidget {
  const Blackwells({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: SafeArea(
        child: Center(
          child: Text('Blackwells'),
        ),
      ),
    );
  }
}
